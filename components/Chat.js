import React from 'react'
import styled from 'styled-components'

const Chat = ({ id, users }) => {
  return (
    <Container>
      <p>Recipient Email</p>
    </Container>
  )
}

export default Chat

const Container = styled.div``
